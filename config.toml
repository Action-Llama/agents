scale = 20

[models.fast]
provider = "openai"
model = "gpt-4o-mini"
authType = "api_key"

[models.medium]
provider = "openai"
model = "gpt-4o"
authType = "api_key"

[models.deep]
provider = "openai"
model = "o1-preview"
authType = "api_key"

[webhooks.github]
type = "github"
credential = "default"
